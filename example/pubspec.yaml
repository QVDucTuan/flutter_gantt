name: example
description: A new Flutter project.
publish_to: 'none'

version: 0.0.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_gantt:
    path: ../
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true